// The array contains the same pattern like the other languages
let listOfPeople = ["Eve", "Mark", "Janice"]

// The for structure follow a different structure, maybe mescling python with javascript
for name: String in listOfPeople {
  print("Hi \(name), how are u?")
}
